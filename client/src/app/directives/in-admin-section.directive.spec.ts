import { InAdminSectionDirective } from './in-admin-section.directive';

describe('InAdminSectionDirective', () => {
  it('should create an instance', () => {
    const directive = new InAdminSectionDirective();
    expect(directive).toBeTruthy();
  });
});
